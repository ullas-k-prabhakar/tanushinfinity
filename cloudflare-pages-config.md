# Cloudflare Pages configuration

This Astro project is ready to deploy on Cloudflare Pages with the following settings:

- Framework preset: None / Static
- Build command: `npm run build`
- Build output directory: `dist`
- Root directory: `/` (project root)

If you want a custom domain, add it in the Cloudflare Pages dashboard after deployment.
